/**
 * Constraints that express account rules, so the rule is stated once and enforced wherever the
 * command is built — a controller today, a just-in-time provisioning listener at step 2.
 *
 * <p>{@link org.jspecify.annotations.NullMarked} is declared here and not inherited from the parent
 * package: Java packages do not nest for annotation purposes.
 */
@NullMarked
package dev.vaullet.auth.account.service.validation;

import org.jspecify.annotations.NullMarked;
